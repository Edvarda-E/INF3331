import setuptools

setuptools.setup(
    name="mandelbrot_package",
    version="1.0",
    author="Edvarda Eriksen",
    description="A mandelbrot package that calculates the mandelbrot set",
    packages=['mandelbrot_package', ]
)
